{% extends "./_layout.njk" %}

{% from "_macros/entity.njk" import Entity with context %}

{% block body_main_content %}
  {% call Example('`entity-list` of `Entity` items') %}
    {{
      CollectionContent(investmentProjects | assign({
        highlightTerm: searchTerm,
        countLabel: 'Investments',
        query: QUERY
      }))
    }}
  {% endcall %}

  <hr/>

  {% call Example('`entity-list` of `Entity` items with `block-links` modifer') %}
    {{
      CollectionContent(investmentProjects | assign({
        highlightTerm: searchTerm,
        countLabel: 'Investments',
        listModifier: 'block-links',
        query: QUERY
      }))
    }}
  {% endcall %}
{% endblock %}
