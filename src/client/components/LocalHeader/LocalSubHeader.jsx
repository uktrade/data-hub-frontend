import styled from 'styled-components'
import { HEADING_SIZES } from '@govuk-react/constants'

const LocalSubHeader = styled('h2')`
  font-size: ${HEADING_SIZES.M}px;
`

export default LocalSubHeader
