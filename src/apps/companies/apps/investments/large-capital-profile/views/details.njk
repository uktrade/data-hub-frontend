<p>Investor details: UI read only fields go here...</p>

{% call Form({
  buttonText: 'Edit',
  hiddenFields: { edit: 'details' },
  buttonDataAttr: 'investor-details-edit',
  disableFormAction: edit === 'requirements' or edit === 'location',
  action: '/companies/' + company.id + '/investments/large-capital-profile/edit'
})
%}
{%- endcall %}
