const project = require('./project')
const equitySource = require('./equity-source')
const investmentType = require('./investment-type')
const start = require('./start')

module.exports = {
  project,
  equitySource,
  investmentType,
  start,
}
