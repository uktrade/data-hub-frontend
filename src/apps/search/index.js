const urls = require('../../lib/urls')
const router = require('./router')

module.exports = {
  displayName: 'Search',
  // mountpath: urls.search.index.mountPoint,
  mountpath: 'fooooooooooooooooo',
  router,
}
