{% extends "_layouts/datahub-base.njk" %}

{% block body_main_content %}
  {{ MetaList({
    items: [
      { label: 'Interaction type', value: interactionTypeLabel }
    ],
    itemModifier: 'stacked'
  }) }}

  {{ Form(editInteractionForm) }}
{% endblock %}
