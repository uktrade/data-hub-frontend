{% extends '_layouts/search.njk' %}

{% block main_grid_right_column %}
  {{
    Results(results | assign({
      el: 'header',
      sort: sort,
      filters: filters,
      highlightTerm: searchTerm,
      countLabel: 'investment project',
      query: QUERY
    }))
  }}
{% endblock %}
