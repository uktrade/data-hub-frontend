import { SectionBreak } from 'govuk-react'
import { SPACING } from '@govuk-react/constants'
import styled from 'styled-components'

export default styled(SectionBreak)({
  marginBottom: SPACING.SCALE_3,
})
