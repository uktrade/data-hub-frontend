const evidenceForm = require('./evidence-form')

// TODO(jf): upload form needs to be specific to each module, not imported from the document module
// const uploadForm = require('../../document-upload/macros/upload-form')

module.exports = {
  evidenceForm,
}
