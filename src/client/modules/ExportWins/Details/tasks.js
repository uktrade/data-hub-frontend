import exportWin from './confirmed.json'

// eslint-disable-next-line no-unused-vars
export const getExportWinDetails = ({ companyId }) => {
  // Stub the endpoint until we have one. Once we have
  // an endpoint then pass the companyId to fetch the
  // export win
  return Promise.resolve({
    exportWin,
  })
}
