version: '3'
services:
  api:
    build: ../data-hub-api
    volumes:
      - ../data-hub-api:/app
    command: /app/start.sh

  celery:
    build: ../data-hub-api
    volumes:
      - ../data-hub-api:/app
