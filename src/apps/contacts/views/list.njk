{% extends "_layouts/two-column.njk" %}

{% block main_grid_right_column %}
  {{
    Collection(results | assign({
      countLabel: 'contact',
      sortForm: sortForm,
      query: QUERY
    }))
  }}
{% endblock %}
